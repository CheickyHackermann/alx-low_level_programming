#include <stdlib.h>
#include <stdio.h>

/** main start the program
 *it always end by
 *return 0 if success
 */
int main(void)
{

	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return(0);

}       
